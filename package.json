{
  "name": "@workgrid/dynamo-query-builder",
  "version": "1.1.8",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn run clean && tsc",
    "prepack": "yarn --dev && yarn run build"
  },
  "files": [
    "dist"
  ],
  "private": false,
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^12.12.20",
    "@typescript-eslint/eslint-plugin": "^2.30.0",
    "@typescript-eslint/parser": "^2.30.0",
    "eslint": "^6.8.0",
    "typescript": "^3.7.3"
  }
}
